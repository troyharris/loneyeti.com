<h1>Listing fiction</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Imagepath</th>
    <th>Externallink</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @fiction.each do |fiction| %>
  <tr>
    <td><%= fiction.name %></td>
    <td><%= fiction.description %></td>
    <td><%= fiction.imagePath %></td>
    <td><%= fiction.externalLink %></td>
    <td><%= link_to 'Show', fiction %></td>
    <td><%= link_to 'Edit', edit_fiction_path(fiction) %></td>
    <td><%= link_to 'Destroy', fiction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Fiction', new_fiction_path %>
